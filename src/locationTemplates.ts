import services from "../data/services.json";
import type { RegionItem, CityItem } from "./sitemaps";

const DOMAIN = "villageplumbers.co.nz";
const PHONE_DISPLAY = "0800 845 524";
const PHONE_HREF = "tel:+64800845524";

const CSS = `
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;font-family:Inter,Arial,sans-serif;color:#0f172a;background:#fff;-webkit-font-smoothing:antialiased}a{color:inherit;text-decoration:none}.wrap{max-width:1180px;margin:auto;padding:0 22px}.top{background:#0b1c2d;color:#fff;font-size:12px}.top .wrap,.nav .wrap{display:flex;justify-space-between;align-items:center;gap:20px}.top .wrap{padding-top:8px;padding-bottom:8px}.top b{color:#fbbf24}.nav{background:#fff;border-bottom:1px solid #e2e8f0;box-shadow:0 7px 28px rgba(15,23,42,.07)}.nav .wrap{padding-top:14px;padding-bottom:14px}.brand{display:flex;align-items:center;gap:11px;font-size:20px;font-weight:900;color:#0b1c2d}.brand small{display:block;color:#64748b;font-size:8px;letter-spacing:.13em;text-transform:uppercase}.logo{display:grid;place-items:center;width:42px;height:42px;border-radius:12px;background:#16b7df;color:#fff;font-weight:900}.links{display:flex;gap:24px;font-size:14px;font-weight:800;color:#334155}.links a:hover{color:#0ea5e9}.call{background:#f47b20;color:#fff;padding:12px 18px;border-radius:10px;font-size:13px;font-weight:900}.hero{position:relative;isolation:isolate;min-height:500px;color:#fff;display:flex;align-items:center;background:#0b1c2d;padding:60px 0}.hero-inner{max-width:780px}.crumb{font-size:13px;color:#cbd5e1}.crumb a{color:#7dd3fc}.pill{display:inline-block;margin-top:20px;padding:8px 13px;border-radius:999px;background:rgba(16,185,129,.15);color:#a7f3d0;font-size:11px;font-weight:900;text-transform:uppercase;letter-spacing:.08em}.hero h1{margin:15px 0 0;font-size:52px;line-height:1.04;letter-spacing:-.03em}.hero h1 em{font-style:normal;color:#16b7df}.hero p{max-width:720px;color:#dbe4ee;font-size:18px;line-height:1.72}.buttons{display:flex;flex-wrap:wrap;gap:13px;margin-top:24px}.btn{display:inline-block;padding:14px 20px;border-radius:10px;font-weight:900}.btn.orange{background:#f47b20;color:#fff}.btn.ghost{border:1px solid rgba(255,255,255,.28);background:rgba(255,255,255,.08);color:#fff}.trust{display:flex;flex-wrap:wrap;gap:20px;margin-top:25px;color:#e2e8f0;font-size:13px;font-weight:800}.trust span:before{content:'✓';margin-right:7px;color:#7dd3fc}.stats{border-bottom:1px solid #e2e8f0}.stats .wrap{display:grid;grid-template-columns:repeat(4,1fr)}.stat{text-align:center;padding:25px 12px;border-left:1px solid #e2e8f0}.stat:first-child{border-left:0}.stat strong{display:block;color:#0b1c2d;font-size:29px}.stat span{display:block;margin-top:4px;color:#64748b;font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.section{padding:72px 0}.soft{background:#f4f7f9}.eyebrow{color:#0284c7;font-size:11px;font-weight:900;text-transform:uppercase;letter-spacing:.14em}.section h2{margin:9px 0 12px;color:#0b1c2d;font-size:38px;line-height:1.12;letter-spacing:-.02em}.muted{color:#64748b;line-height:1.75}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:34px}.card{display:block;background:#fff;border:1px solid #e2e8f0;border-radius:16px;padding:24px;box-shadow:0 4px 15px rgba(15,23,42,.035);transition:.2s}.card:hover{transform:translateY(-3px);box-shadow:0 18px 38px rgba(15,23,42,.09);border-color:#7dd3fc}.icon{display:grid;place-items:center;width:42px;height:42px;border-radius:11px;background:#e0f2fe;color:#0284c7;font-weight:900}.card h3{margin:18px 0 0;font-size:19px}.card p{color:#64748b;font-size:14px;line-height:1.65}.more{color:#0284c7;font-size:14px;font-weight:900}.directory{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-top:24px}.directory a{display:flex;align-items:center;justify-content:space-between;padding:14px 16px;background:#fff;border:1px solid #e2e8f0;border-radius:12px;font-weight:800;color:#334155;transition:.18s}.directory a:after{content:'→';color:#0284c7}.directory a:hover{border-color:#7dd3fc;color:#0284c7;transform:translateY(-2px)}.content{display:grid;grid-template-columns:minmax(0,1fr) 310px;gap:44px}.article h2{font-size:28px;margin-top:36px}.article p,.article li{color:#64748b;line-height:1.8}.sidebar{display:grid;gap:16px;align-self:start;position:sticky;top:20px}.sidebox{border-radius:16px;background:#0b1c2d;color:#fff;padding:23px}.sidebox.green{background:#ecfdf5;color:#065f46}.sidebox.light{background:#f4f7f9;color:#0f172a}.sidebox h3{margin-top:0;color:inherit}.sidebox p{font-size:14px;line-height:1.65;color:inherit}.sidebox .btn{display:block;text-align:center;margin-top:12px}.faq{max-width:830px;margin:32px auto 0}.faq details{background:#fff;border:1px solid #e2e8f0;border-radius:10px;padding:16px 18px;margin:9px 0}.faq summary{cursor:pointer;font-weight:900;color:#0b1c2d}.faq p{color:#64748b;line-height:1.65}.footer{background:#0b1c2d;color:#cbd5e1;padding:54px 0 23px}.footgrid{display:grid;grid-template-columns:1.2fr .8fr .9fr 1.1fr;gap:35px}.footer h3{color:#fff}.footer p,.footer a{color:#94a3b8;font-size:14px;line-height:1.75}.footer .stack{display:grid;gap:8px}.legal{border-top:1px solid #1e293b;margin-top:36px;padding-top:20px;color:#64748b;font-size:12px}.popup-trigger{position:fixed;right:20px;bottom:20px;z-index:90;border:0;border-radius:999px;background:#f47b20;color:#fff;padding:14px 20px;font-weight:900;box-shadow:0 14px 35px rgba(15,23,42,.25);cursor:pointer}.modal{display:none;position:fixed;inset:0;z-index:100;background:rgba(2,6,23,.72);padding:20px;align-items:center;justify-content:center}.modal.open{display:flex}.modal-card{position:relative;width:min(760px,100%);border-radius:20px;overflow:hidden;background:#fff;display:grid;grid-template-columns:1fr 1fr;box-shadow:0 30px 90px rgba(0,0,0,.35)}.modal-copy{background:#0b1c2d;color:#fff;padding:34px}.modal-copy h2{font-size:30px;margin-top:0}.modal-copy p{color:#cbd5e1;line-height:1.7}.modal-form{padding:34px}.modal-form label{display:block;margin-top:12px;color:#334155;font-size:12px;font-weight:900}.modal-form input,.modal-form select{width:100%;margin-top:5px;border:1px solid #cbd5e1;border-radius:99px;padding:12px}.modal-form button{width:100%;margin-top:17px;border:0}.close{position:absolute;right:13px;top:10px;border:0;background:transparent;font-size:24px;cursor:pointer;color:#64748b}@media(max-width:900px){.links{display:none}.grid,.directory,.content,.footgrid{grid-template-columns:1fr}.sidebar{position:static}.modal-card{grid-template-columns:1fr}}@media(max-width:580px){.stats .wrap{grid-template-columns:1fr 1fr}.stat:nth-child(3){border-left:0;border-top:1px solid #e2e8f0}.stat:nth-child(4){border-top:1px solid #e2e8f0}.hero h1{font-size:36px}}
`;

function escapeHtml(value: string) {
  return value.replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[char] ?? char);
}

function header() {
  return `<header><div class="top"><div class="wrap"><span>● &nbsp; New Zealand Plumbing &amp; Trade Referral Directory</span><span><b>Certificated Trade Specialists</b> &nbsp; | &nbsp; Call ${PHONE_DISPLAY}</span></div></div><div class="nav"><div class="wrap"><a class="brand" href="https://${DOMAIN}/"><span class="logo">VP</span><span>Village Plumbers<small>New Zealand · Emergency &amp; Maintenance</small></span></a><nav class="links"><a href="https://${DOMAIN}/">Home</a><a href="https://${DOMAIN}/services">Services</a><a href="https://${DOMAIN}/areas-we-serve">Service Areas</a><a href="https://${DOMAIN}/about">About</a><a href="https://${DOMAIN}/contact">Contact</a></nav><a class="call" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a></div></div></header>`;
}

function footer() {
  return `<footer class="footer"><div class="wrap"><div class="footgrid"><div><a class="brand" style="color:#fff" href="https://${DOMAIN}/"><span class="logo">VP</span><span>Village Plumbers NZ</span></a><p>Independent plumbing trade directory organized by service, region, and suburb across New Zealand.</p></div><div><h3>Services</h3><div class="stack"><a href="https://${DOMAIN}/emergency-plumbing">Emergency Plumber</a><a href="https://${DOMAIN}/blocked-drain-unblocking">Blocked Drains</a><a href="https://${DOMAIN}/burst-pipe-repair">Burst Pipes</a><a href="https://${DOMAIN}/hot-water-cylinder-repair">Hot Water Cylinders</a><a href="https://${DOMAIN}/services">All Services →</a></div></div><div><h3>Service Areas</h3><div class="stack"><a href="https://${DOMAIN}/areas-we-serve">All Regions</a><a href="https://${DOMAIN}/about">About Us</a><a href="https://${DOMAIN}/contact">Contact</a></div></div><div><h3>Legal &amp; Disclosure</h3><div class="stack"><a href="https://${DOMAIN}/privacy-policy">Privacy Policy</a><a href="https://${DOMAIN}/terms">Terms</a><a href="https://${DOMAIN}/disclaimer">Disclaimer</a><a href="https://${DOMAIN}/provider-disclosure">Provider Disclosure</a></div></div></div><div class="legal">© ${new Date().getUTCFullYear()} Village Plumbers NZ. Providers are independent businesses. Verify licensing, insurance, credentials, pricing, and service terms before hiring.</div></div></footer>`;
}

function popup() {
  return `<button class="popup-trigger" data-open-lead>📞 Call or Request Help</button><div class="modal" id="lead-modal" role="dialog" aria-modal="true" aria-label="Request plumbing help"><div class="modal-card"><button class="close" type="button" data-close-lead aria-label="Close">×</button><div class="modal-copy"><h2>Need Local Plumbing Help?</h2><p>Call our independent plumber hotline directly or enter your details below for a quick callback and local availability check.</p><a class="btn orange" style="display:block;text-align:center;margin-top:18px" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a></div><form class="modal-form" method="get" action="https://${DOMAIN}/contact"><label>Full Name<input name="name" autocomplete="name" required></label><label>Phone Number<input name="phone" type="tel" autocomplete="tel" required></label><label>Suburb or City<input name="location" autocomplete="postal-code" required></label><label>Service Needed<select name="service"><option value="">Select one</option>${services.slice(0, 10).map((s) => `<option value="${escapeHtml(s.slug)}">${escapeHtml(s.name)}</option>`).join("")}</select></label><button class="btn orange" type="submit">Continue Request</button></form></div></div><script>(()=>{const modal=document.getElementById('lead-modal');const open=()=>{modal?.classList.add('open');sessionStorage.setItem('vpPopupSeen','1')};const close=()=>modal?.classList.remove('open');document.querySelectorAll('[data-open-lead]').forEach(el=>el.addEventListener('click',open));document.querySelectorAll('[data-close-lead]').forEach(el=>el.addEventListener('click',close));modal?.addEventListener('click',e=>{if(e.target===modal)close()});if(!sessionStorage.getItem('vpPopupSeen'))setTimeout(open,8000)})();</script>`;
}

function shell(title: string, description: string, canonical: string, content: string, schema: unknown) {
  const safeSchema = JSON.stringify(schema).replaceAll('<', '\\u003c');
  return `<!doctype html><html lang="en-NZ"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(title)} | Village Plumbers NZ</title><meta name="description" content="${escapeHtml(description)}"><meta name="robots" content="index, follow"><link rel="canonical" href="${canonical}"><meta property="og:title" content="${escapeHtml(title)} | Village Plumbers NZ"><meta property="og:description" content="${escapeHtml(description)}"><meta property="og:url" content="${canonical}"><meta property="og:type" content="website"><script type="application/ld+json">${safeSchema}</script><style>${CSS}</style></head><body>${header()}<main>${content}</main>${footer()}${popup()}</body></html>`;
}

export function regionPage(region: RegionItem, host: string) {
  const canonical = `https://${host}/`;
  const suburbLinks = region.cities.map((city) => `<a href="https://${city.subdomain}.${DOMAIN}/"><span>${escapeHtml(city.name)}</span></a>`).join("");
  const schema = {
    "@context": "https://schema.org",
    "@graph": [
      { "@type": "CollectionPage", url: canonical, name: `Plumbing Services in ${region.name}`, about: { "@type": "AdministrativeArea", name: region.name } },
      { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Service Areas", item: `https://${DOMAIN}/areas-we-serve` }, { "@type": "ListItem", position: 2, name: region.name, item: canonical }] },
    ],
  };
  const content = `<section class="hero"><div class="wrap"><div class="hero-inner"><div class="crumb"><a href="https://${DOMAIN}/">Home</a> / <a href="https://${DOMAIN}/areas-we-serve">Service Areas</a> / ${escapeHtml(region.name)}</div><span class="pill">New Zealand Plumbing Directory</span><h1>Plumbing Services across <em>${escapeHtml(region.name)}</em></h1><p>Choose a suburb or town, review local plumbing topics, emergency response considerations, and confirm independent certificated provider availability.</p><div class="buttons"><a class="btn orange" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a><a class="btn ghost" href="#suburbs">Browse ${region.cities.length} Suburbs</a></div></div></div></section><section class="stats"><div class="wrap"><div class="stat"><strong>${services.length}</strong><span>Plumbing topics</span></div><div class="stat"><strong>${region.cities.length}</strong><span>Suburbs & towns</span></div><div class="stat"><strong>${escapeHtml(region.name)}</strong><span>Region hub</span></div><div class="stat"><strong>Direct</strong><span>Provider check</span></div></div></section><section class="section soft" id="suburbs"><div class="wrap"><div class="eyebrow">Service locations</div><h2>Suburbs &amp; communities in ${escapeHtml(region.name)}</h2><div class="directory">${suburbLinks}</div></div></section>`;
  return shell(`Plumbing Services in ${region.name}`, `Browse plumbing services and suburb routes across ${region.name}, New Zealand.`, canonical, content, schema);
}

export function suburbPage(region: RegionItem, city: CityItem, host: string) {
  const canonical = `https://${host}/`;
  const cards = services.slice(0, 12).map((service, index) => `<a class="card" href="https://${host}/${service.slug}"><span class="icon">0${index + 1}</span><h3>${escapeHtml(service.name)}</h3><p>${escapeHtml(service.description)}</p><span class="more">View local service →</span></a>`).join("");
  const schema = {
    "@context": "https://schema.org",
    "@graph": [
      { "@type": "CollectionPage", url: canonical, name: `Plumber in ${city.name}, ${region.name}`, about: { "@type": "City", name: city.name } },
      { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: region.name, item: `https://${region.code}.${DOMAIN}/` }, { "@type": "ListItem", position: 2, name: city.name, item: canonical }] },
    ],
  };
  const content = `<section class="hero"><div class="wrap"><div class="hero-inner"><div class="crumb"><a href="https://${region.code}.${DOMAIN}/">${escapeHtml(region.name)}</a> / ${escapeHtml(city.name)}</div><span class="pill">Local plumbing guide</span><h1>Plumber in <em>${escapeHtml(city.name)}, ${escapeHtml(region.name)}</em></h1><p>Compare plumbing services, blocked drain clearing, hot water cylinder repair, and emergency response for ${escapeHtml(city.name)} properties.</p><div class="buttons"><a class="btn orange" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a><a class="btn ghost" href="#services">View Services</a></div></div></div></section><section class="stats"><div class="wrap"><div class="stat"><strong>${services.length}</strong><span>Services</span></div><div class="stat"><strong>${escapeHtml(region.name)}</strong><span>Region</span></div><div class="stat"><strong>${escapeHtml(city.name)}</strong><span>Suburb</span></div><div class="stat"><strong>24/7</strong><span>Call hotline</span></div></div></section><section class="section soft" id="services"><div class="wrap"><div class="eyebrow">Local Plumbing Services</div><h2>Services available in ${escapeHtml(city.name)}</h2><div class="grid">${cards}</div></div></section>`;
  return shell(`Plumber in ${city.name}, ${region.name}`, `Emergency plumbing, blocked drains, burst pipes, and hot water cylinder services for ${city.name}, ${region.name}.`, canonical, content, schema);
}

export function localServicePage(region: RegionItem, city: CityItem, service: (typeof services)[number], host: string) {
  const canonical = `https://${host}/${service.slug}`;
  const schema = {
    "@context": "https://schema.org",
    "@graph": [
      { "@type": "Service", url: canonical, name: `${service.name} in ${city.name}, ${region.name}`, serviceType: service.category },
      { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: region.name, item: `https://${region.code}.${DOMAIN}/` }, { "@type": "ListItem", position: 2, name: city.name, item: `https://${host}/` }, { "@type": "ListItem", position: 3, name: service.name, item: canonical }] },
    ],
  };
  const content = `<section class="hero"><div class="wrap"><div class="hero-inner"><div class="crumb"><a href="https://${region.code}.${DOMAIN}/">${escapeHtml(region.name)}</a> / <a href="https://${host}/">${escapeHtml(city.name)}</a> / ${escapeHtml(service.name)}</div><span class="pill">${escapeHtml(service.category)}</span><h1>${escapeHtml(service.name)} in <em>${escapeHtml(city.name)}, ${escapeHtml(region.name)}</em></h1><p>${escapeHtml(service.description)} Review warning signs, inspection scope, and confirm local certificated provider availability.</p><div class="buttons"><a class="btn orange" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a></div></div></div></section><section class="section"><div class="wrap content"><article class="article"><h2>Professional ${escapeHtml(service.name.toLowerCase())} in ${escapeHtml(city.name)}</h2><p>A reliable service starts with accurate diagnosis of pipework, drainage, water pressure, or hot water cylinder systems. Review signs of failure and confirm credentials before authorizing work.</p></article><aside class="sidebar"><div class="sidebox"><h3>Need Plumbing Help?</h3><p>${escapeHtml(city.name)}, ${escapeHtml(region.name)}</p><a class="btn orange" href="${PHONE_HREF}">${PHONE_DISPLAY}</a></div></aside></div></section>`;
  return shell(`${service.name} in ${city.name}, ${region.name}`, `${service.description} Review service information for ${city.name}, ${region.name}.`, canonical, content, schema);
}

export function notFoundPage(message: string) {
  return `<!doctype html><html><head><meta name="robots" content="noindex"><title>404 | Village Plumbers NZ</title><style>${CSS}</style></head><body>${header()}<main class="section"><div class="wrap"><span class="eyebrow">Page Not Found</span><h1>404</h1><p>${escapeHtml(message)}</p><a class="btn orange" href="https://${DOMAIN}/areas-we-serve">Browse Service Areas</a></div></main>${footer()}</body></html>`;
}
